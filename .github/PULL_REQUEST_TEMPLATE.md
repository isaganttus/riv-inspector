## What changed

<!-- Briefly describe what you added, fixed, or changed -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] New CLI flags are documented in `usage()` and the README options table
- [ ] New output fields are covered by a test
- [ ] `rive-metadata-reference.md` updated if the WASM API surface changed
